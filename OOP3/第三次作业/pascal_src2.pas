begin
  UnRegisterHotKey(handle, HotKeyId);
    if (EditScript.Text <> '') then
      begin
	    else
		  FileExists(inifile);
        if FileExists(inifile) then DeleteFile(inifile);
          ini := TIniFile.Create(inifile);
          ini.WriteString('0', 'Script', EditScript.Text);
          ini.WriteString('0', 'HotKey', ShortCutToText(HotKey1.HotKey));
		if 1
      end;
	    then  writein('a=',a);
end